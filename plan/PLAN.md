# PLAN: milestone index

The ordering authority. Milestones are listed here in the order they are worked;
their folders are named `NNNN-slug` (zero-padded, and the number is identity, not a
sort key; see `call/0000`). A milestone is a thin, persona-serving increment.

No milestones yet. The host adopted the methodology on 2026-07-02 (see
`call/0000`); the first milestone is added when persona-serving work on
UDPspeeder-simd begins. Build that milestone with `host-lifecycle next plan/` for
the number, name it after its content, and link it here.
