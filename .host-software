# .host-software — the Where room: each component a bare store with worktrees.
# Tracked recipe; the trees under software/ are local, gitignored, materialised by
# `host-lifecycle software --materialize .` and audited by `--check`.

# The gating tool. Consumed (referenced), not authored here, so it carries the
# commit hook dispatch and the built binary it installs, but no build recipe: the
# host does not claim to reproduce host-lint, only to pin and install it.
[software "host-lint"]
    url      = https://github.com/connollydavid/host-lint.git
    pin      = 966259fb2001d0a4f6bdf3ae608ddd1c36c309d0
    branch   = main
    hooks    = pre-commit
    artifact = target/release/host-lint 5ea6e94d17e7a860c9809728eee988f23caffe45e2d958628835fd44271ec645
    repro-waiver = call/0002

# The software under development. Migrated (pre-existing), recorded as a source pin
# on its canonical branch with no deploy artifact, so no reproducible build is
# claimed yet (add a recipe + repro-exempt, or a real artifact, before shipping).
[software "udpspeeder-simd"]
    url    = https://github.com/connollydavid/UDPspeeder-simd.git
    pin    = 4370bac
    branch = main
    # This component publishes release binaries but records no build recipe or
    # artifact hash, because the build is not yet deterministic: the native line
    # uses whatever g++ the runner image carried. Recording a hash for a build
    # that cannot be reproduced would claim a guarantee we have not established.
    # call/0006 states the interim provenance and what retires this.
    repro-waiver = call/0006

# A fork of the OpenWrt packages feed (Makefiles/shell/C). Migrated (pre-existing),
# recorded as a source pin on its canonical branch (master) with no deploy artifact:
# it is a package-build-recipe feed layered on an OpenWrt buildroot, not a built
# artifact, so no reproducible build is claimed here. GPL-2.0 in its own tree.
[software "packages"]
    url    = https://github.com/connollydavid/packages.git
    pin    = 1d40ad929ad45cfbdf547996d0ba2379d9765172
    branch = master
    # The net/udpspeeder version bump offered to openwrt/packages, as its own
    # worktree rather than a checkout of the canonical one, so master stays at
    # its pin and both are inspectable at once.
    worktree = udpspeeder-update-20260731 d9ef24250436f3557a851573abaa0468a8df5c09
    # The published feed, one branch per OpenWrt release line, snapshot
    # included. None is offered upstream. Each carries four packages,
    # net/udpspeeder-snapshot, net/udpspeeder-simd-snapshot and the two from
    # utils/flashprog-snapshot, which take names of their own rather than the
    # ones OpenWrt ships or is being asked to ship. It is published at
    # https://connollydavid.github.io/UDPspeeder-simd/feed and rebuilt weekly.
    worktree = feed-25.12 387ae05fc4da3f532c17a2f7a70b92878b95959c
    worktree = feed-24.10 387ae05fc4da3f532c17a2f7a70b92878b95959c
    worktree = feed-main 387ae05fc4da3f532c17a2f7a70b92878b95959c
    # The flashprog package offered to openwrt/packages in issue 29591, and the
    # branch carrying the matrix that verifies it. The latter is ours alone and
    # is never offered upstream.
    worktree = flashprog 67d370dcc6b91603139ac55d0db79214eca44039
    worktree = ci-flashprog 75ff54251411ef5f8182d76a0d93e725b04d40e3

# OpenWrt upstream (the buildroot). Embedded as a source pin on its canonical branch
# (main; the master branch mirrors the same commit) so the x86_64 SDK can be built from
# source and the udpspeeder-simd feed package validated against the snapshot SDK the
# OpenWrt CI uses. GPL-2.0 in its own tree. No artifact recorded here: the SDK build is a
# separate, heavy step, not a reproducible-build claim of this host.
[software "openwrt"]
    url    = https://github.com/openwrt/openwrt.git
    pin    = 62e7565f0e1e88e5fc8d038f09cb38ffd583bbed
    branch = main
