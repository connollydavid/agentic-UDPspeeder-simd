[receipt "classify"]
    disposition = done
    evidence = case (a): no CLAUDE.md, no .host stamp; rename map empty
    tool = host-lifecycle@0.35.1
    recorded = 2026-07-02

[receipt "embed" "host-lint"]
    disposition = done
    evidence = software/host-lint/main at pin 78804cd; hooks installed
    tool = host-lifecycle@0.35.1
    recorded = 2026-07-02

[receipt "embed" "udpspeeder-simd"]
    disposition = done
    evidence = software/udpspeeder-simd/branch_libev at pin 3374e3b
    tool = host-lifecycle@0.35.1
    recorded = 2026-07-02

[receipt "remap"]
    disposition = done
    evidence = no ordinal tells; host-lint --all clean; no dictionary needed
    tool = host-lifecycle@0.35.1
    recorded = 2026-07-02

[receipt "publish"]
    disposition = skip
    reason = mdBook site generation not yet configured for this host
    tool = host-lifecycle@0.35.1
    recorded = 2026-07-02

[receipt "release" "host-lint"]
    disposition = skip
    reason = host-lint is a consumed tool, not released through this host
    tool = host-lifecycle@0.35.1
    recorded = 2026-07-02

[receipt "release" "udpspeeder-simd"]
    disposition = skip
    reason = no binary release of udpspeeder-simd cut through this host yet
    tool = host-lifecycle@0.35.1
    recorded = 2026-07-02

[receipt "remap"]
    disposition = skip
    reason = case (a) adoption: no ordinal-named concepts to rename; host-lint --all is clean
    tool = host-lifecycle@0.35.1
    recorded = 2026-07-02

[receipt "verify"]
    disposition = done
    evidence = validate plan/call ok; prose clean; reconcile clean; software at pins; hooks installed
    tool = host-lifecycle@0.35.1
    recorded = 2026-07-02

[receipt "embed" "packages"]
    disposition = done
    evidence = software/packages/master at pin 1d40ad9; migrated source-pin feed, worktree clean
    tool = host-lifecycle@0.35.1
    recorded = 2026-07-03

[receipt "release" "packages"]
    disposition = skip
    reason = no release of the packages feed cut through this host (migrated source-pin OpenWrt feed)
    tool = host-lifecycle@0.35.1
    recorded = 2026-07-03

[receipt "publish"]
    disposition = done
    evidence = book . -> 17 pages; book --check passes (every room renders); mdbook v0.4.40 builds mdBook/out; Site lane at .github/workflows/mdbook.yml deploys mdBook/out to gh-pages
    tool = host-lifecycle@0.35.1
    recorded = 2026-07-03

[receipt "embed" "openwrt"]
    disposition = done
    evidence = software/openwrt/main at pin 62e7565 (symlinked to ext4 store for case-sensitive build); consumed upstream, source pin
    tool = host-lifecycle@0.35.1
    recorded = 2026-07-03

[receipt "release" "openwrt"]
    disposition = skip
    reason = openwrt upstream is consumed to build the SDK, not released through this host
    tool = host-lifecycle@0.35.1
    recorded = 2026-07-03
